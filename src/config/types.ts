// SPDX-License-Identifier: LicenseRef-Blockscout

// Operator-facing encoding: the first item is the light color mode value, the second the dark one.
// A single-item array applies to both modes.
export type ColorModeValue = Array<string | undefined>;
