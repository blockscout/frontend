# Open questions — Rewrite the .mjs scripts under tools/ in TypeScript

None. Every decision was settled in the grilling session: the task is developer tooling with no
product, design, API or env surface, so no question needed an owner outside the session and nothing
was routed to Slack.

Add an entry here (`Q01`, `Q02`, …) if one arises during implementation.
