# Open questions — Rework how Playwright component tests are selected and run in CI

No open questions. Every decision was settled in the grilling session that produced `spec.md`; nothing is
routed to a colleague. Add entries here (`Q01`, …) if an implementation ticket surfaces one.
