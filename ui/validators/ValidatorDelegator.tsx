/* eslint-disable */

import React from "react";

const ValidatorDelegator = () => {

    React.useEffect(() => {
        // This is a placeholder for any side effects you want to run when the component mounts
        console.log("ValidatorDelegator component mounted");
    } , []);

    return (
        <div>
            <h1>Validator Delegator</h1>
        </div>
    );
}

export default ValidatorDelegator;

